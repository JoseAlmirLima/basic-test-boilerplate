{
  "name": "front-end-almir",
  "version": "1.0.0",
  "description": "Simple Parcel Sandbox",
  "main": "index.html",
  "scripts": {
    "start": "parcel index.html --open",
    "build": "parcel build index.html",
    "deploy": "yarn build && gh-pages -d ./dist"
  },
  "dependencies": {
    "parcel": "1.12.3",
    "surge": "^0.23.0"
  },
  "devDependencies": {
    "parcel-bundler": "^1.6.1",
    "sass": "^1.43.3"
  },
  "keywords": []
}
